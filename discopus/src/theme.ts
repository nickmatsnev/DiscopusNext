// src/theme.ts

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#64b5f6',
    },
    secondary: {
      main: '#e3f2fd',
    },
  },
});

export default theme;